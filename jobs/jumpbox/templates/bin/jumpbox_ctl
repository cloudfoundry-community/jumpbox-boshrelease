#!/bin/bash

HOME_DIR=/var/vcap/store/jumpbox/home

case $1 in

  start)
    mkdir -p $HOME_DIR
    chown vcap:vcap $HOME_DIR
    chpst -u vcap:vcap touch $HOME_DIR/.jumpbox_test

    mkdir -p $HOME_DIR/tmp

    usermod -d $HOME_DIR -m vcap

    if [ ! -e /etc/profile.d/bosh.sh ]; then
      echo 'export PATH=/var/vcap/packages/jumpbox/bin:/var/vcap/packages/ruby-2.1.6/bin:$PATH' >> /etc/profile.d/bosh.sh
      echo "export TMPDIR=$HOME_DIR/tmp" >> /etc/profile.d/bosh.sh
      chmod +x /etc/profile.d/bosh.sh
    fi
    ;;

  stop)

    ;;

  *)
    echo "Usage: jumpbox_ctl {start|stop}"
    ;;

esac
